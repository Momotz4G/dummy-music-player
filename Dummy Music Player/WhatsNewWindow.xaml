<Window x:Class="Dummy_Music_Player.WhatsNewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="What's New" Height="350" Width="400"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="What's New in Simple Music Player!" 
                   FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" x:Name="VersionText" Text="Version 1.x.x" 
                   FontSize="14" Foreground="Gray" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <TextBox Grid.Row="2" x:Name="FeaturesText" 
                 Text="• New features go here"
                 IsReadOnly="True" 
                 TextWrapping="Wrap" 
                 VerticalScrollBarVisibility="Auto" 
                 BorderThickness="0" 
                 Background="Transparent"
                 Margin="10,0,10,10"/>

        <Button Grid.Row="3" Name="OkButton" Content="OK" 
                Width="100" HorizontalAlignment="Center" 
                Click="OkButton_Click" IsDefault="True"/>
    </Grid>
</Window>