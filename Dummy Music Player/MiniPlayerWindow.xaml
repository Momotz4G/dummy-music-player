<Window x:Class="Dummy_Music_Player.MiniPlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Simple Music Player" Height="100" Width="380"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" Topmost="True" ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="#F0EFEFEF" CornerRadius="8" BorderBrush="#888888" BorderThickness="1">
        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Image x:Name="AlbumArtImage" Width="90" Height="90" Margin="5" Stretch="Uniform"/>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">

                <TextBlock x:Name="SongTitleText" Text="No Song Selected" FontSize="14" FontWeight="Bold"
                           TextTrimming="CharacterEllipsis" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                <TextBlock x:Name="ArtistAlbumText" Text="Artist - Album" FontSize="11"
                           TextTrimming="CharacterEllipsis" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>

                <Separator Margin="0,5"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="PrevButton" Content="◀◀" Margin="5" Padding="10,5" Click="PrevButton_Click"/>
                    <Button x:Name="PlayButton" Content="▶" Margin="5" Padding="10,5" Click="PlayButton_Click"/>
                    <Button x:Name="NextButton" Content="▶▶" Margin="5" Padding="10,5" Click="NextButton_Click"/>
                    <Button x:Name="MaximizeButton" Width="30" Margin="15,5,5,5" Padding="5" 
                        Click="MaximizeButton_Click" ToolTip="Show Full Player">
                        <Image Source="Assets/Mini-player_maximize_button.png" Width="12" Height="12" />
                    </Button>
                </StackPanel>

            </StackPanel>
        </Grid>
    </Border>
</Window>